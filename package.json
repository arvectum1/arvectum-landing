{
  "name": "arvectum-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --port 8788",
    "lint": "npm run lint:format && npm run lint:js && npm run lint:php",
    "lint:format": "prettier --check \"public/**/*.{html,css,js}\" README.md",
    "lint:js": "node -e \"const fs=require('fs'); ['public/app.js','public/site-config.js'].forEach((file)=>new Function(fs.readFileSync(file,'utf8'))); console.log('JS syntax OK')\"",
    "lint:php": "php -l public/api/submit.php && php -l public/api/cookie-consent.php"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
